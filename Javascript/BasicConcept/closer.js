// Clouser Example - https://www.youtube.com/watch?v=71AtaJpJHw0

// In Javascript any variable we can declared outside of the function and that we can access inside the function. But we can't
// access that value outside function to the inside function

// Beacause javascript we called something lexical scoping
// Variable inside can't be accessable outside
function Closer() {

}

