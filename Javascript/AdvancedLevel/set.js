// Javascipt Set

// In ES5 Array lot of things are not possible which solved the Set concept, has been introduce in set
// You can store Arbitary Value inside set

// Diff, array and set is that while an array can have duplicate values a set can’t. Elements can be accessed in array using index which isn’t possible in Set since it uses keys and elements can be traversed only in the way they were entered.

// const arr = [NaN];

// console.log(arr.indexOf('NaN'))


// const set = new Set();

// set.add('red');
// set.add('blue');
// set.add('yellow');
// set.add('yellow');

// console.log(set.has('red'));
// console.log(set);

// const setEntries = new Set(['apple', 'bowl', 'cat']);
// const mapEntries = new Map(setEntries.entries());
// console.log(mapEntries)
