Here you go 🚀 — short definitions + UML symbols (the exact ones you’ll see in diagrams).

Aggregation (○—)
➡ A “has-a” relationship where the part can exist without the whole.
(Hollow diamond at the whole side)

Association (—)
➡ A simple connection or link between two independent classes.
(Plain solid line)

Composition (◆—)
➡ A strong “has-a” relationship where the part’s lifetime depends on the whole.
(Filled diamond at the whole side)

Inheritance (△—)
➡ An “is-a” relationship where a child class derives from a parent class.
(Solid line with hollow triangle at parent side)

Dependency ( - - > )
➡ A “uses” relationship where one class relies on another temporarily.
(Dashed line with arrow)

Realization ( - -▷ )
➡ A relationship where a class implements an interface (contract).
(Dashed line with hollow triangle at interface side)